import styled from "styled-components";
import AppBar from "@material-ui/core/AppBar";

export const StyledMenu = styled(AppBar)`
  position: sticky;
  
  .toolbar{
    justify-content: space-between;
    background-color: #000;
    margin: 0;
  }
`;